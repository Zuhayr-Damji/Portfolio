import React from "react";
import style from "./CTA.module.css"

export default function CTA(){
    return (
        <button 
            className={style.CTA}>
            Contact
        </button>
    );
}